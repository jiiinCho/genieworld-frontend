import React from "react";

const ColorEditCard = () => {
  //[todo] seperate Avatr edit card to be reusable in geneiroom editor
  // const [previewColor, setPreviewColor] = useState<CharacterColors>(colors);
  // const [color, setColor] = useState<CharacterColors>(colors);

  return <h1>hello</h1>;
};
export default ColorEditCard;
